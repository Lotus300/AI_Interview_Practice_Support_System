import { state } from "../core/state.mjs";
import { esc, field, select } from "../core/html.mjs";
import { layout } from "./layout.mjs";

export function renderCondition() {
  return layout(`<form data-form="condition" class="condition-layout"><div class="form-stack"><section class="card"><div class="card-heading"><div class="section-icon">01</div><div><h2>面接の種類</h2><p>今回想定する面接を選択してください。</p></div></div>${select("interviewType", "面接種別", "転職活動", ["就職活動", "転職活動", "社内面接"])}</section><section class="card"><div class="card-heading"><div class="section-icon">02</div><div><h2>応募先・テーマ</h2><p>質問の内容をあなたの目的に合わせます。</p></div></div><div class="form-grid">${field("jobRole", "職種（必須）", state.profile?.desiredRole || "Webエンジニア", "text", "required")}${field("industry", "業界（必須）", "IT・Web", "text", "required")}${field("companyName", "企業名（任意）")}${select("theme", "練習テーマ", "総合面接", ["自己PR", "志望動機", "職務経歴", "総合面接"])}</div></section><section class="card"><div class="card-heading"><div class="section-icon">03</div><div><h2>練習ボリューム</h2><p>1〜20問の範囲で選択できます。</p></div></div>${field("questionCount", "質問数", "5", "number", "min=1 max=20 required")}</section></div><aside class="card summary-card"><p class="eyebrow">INTERVIEW PLAN</p><h2>この内容で練習</h2><ul><li><span>プロフィール</span><strong>${esc(state.profile?.fullName || state.user?.name)}</strong></li><li><span>希望職種</span><strong>${esc(state.profile?.desiredRole || "未設定")}</strong></li><li><span>回答方法</span><strong>音声 / テキスト</strong></li></ul><p class="privacy-note">♢ 録音した音声は文字起こし後に破棄されます。</p><button class="primary large full" ${state.busy ? "disabled" : ""}>面接を開始する <span>→</span></button></aside></form>`, { eyebrow: "NEW INTERVIEW", title: "面接条件を設定" });
}
